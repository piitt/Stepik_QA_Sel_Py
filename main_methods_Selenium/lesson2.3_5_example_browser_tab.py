# переключение на нужную вкладку браузера
browser.switch_to.window(window_name)

# получение имени вкладки
new_window = browser.window_handles[1]

# получение имени вкладки
current_window = browser.window_handles[0]
